// ltl-fir-filter/main.ltl
//
// FIR windowed-sinc filter with zero-phase option.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-fir-filter v0.1.0")
    io::println("FIR windowed-sinc filter with zero-phase option.")
    0
}
